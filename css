* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

.game {
    width: 100%;
    height: 500px;

    border-bottom: 15px solid #35a936;

    margin: 0 auto;

    position: relative;
    overflow: hidden;

    background: linear-gradient(#87ceeb, #e0f6ff);
}

.mario {
    width: 150px;

    position: absolute;
    bottom: 0;
    left: 50px;
}

.pipe {
    width: 80px;

    position: absolute;
    bottom: 0;
    right: -80px;

    animation: pipe-animation 2s infinite linear;
}

@keyframes pipe-animation {

    from {
        right: -80px;
    }

    to {
        right: 100%;
    }

}

.jump {
    animation: jump 500ms ease-out;
}

@keyframes jump {

    0% {
        bottom: 0;
    }

    40% {
        bottom: 180px;
    }

    50% {
        bottom: 180px;
    }

    60% {
        bottom: 180px;
    }

    100% {
        bottom: 0;
    }

}